<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Put Your Opinion</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col-md-6 offset-md-3 my-5">
                <div class="card card-body">
                    <h5>Title : <%= poll.title %></h5>
                    <p>Description : <%= poll.description %></p>
                    <div class="my-3">
                        <h5>Result : </h5>
                        <ul class="list-group">
                            <% for(let r of result) { %>
                                <li class="list-group-item">
                                    <p>
                                        <%= r.name %> ---> <strong><%= r.percentage.toFixed(2) %> %</strong>
                                    </p>
                                </li>
                            <% } %>
                        </ul>                        
                    </div>
                    <div class="my-4">
                        <h5>What is Your Opinion?</h5>
                        <form action="/polls/<%= poll._id %>" method="POST">
                            <% for(let option of poll.options) {%>
                                <div class="custom-control custom-radio">
                                    <input type="radio" id="<%= option._id %>" name="option" value="<%= option._id %>" class="custom-control-input">
                                    <label class="custom-control-label" for="<%= option._id %>">
                                        <%= option.name %>
                                    </label>
                                </div>
                            <% } %>
                            <input type="submit" value="Submit Your Opinion" class="btn btn-primary my-3">
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>